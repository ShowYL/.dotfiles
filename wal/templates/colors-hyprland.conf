# Hyprland gradient colors generated by pywal
# Gradient border for active window
$activeBorder = 0xff{color4.strip} 0xff{color6.strip} 0xff{color1.strip} 0xff{color3.strip} 45deg

# Inactive border - very subtle
$inactiveBorder = 0x30948D9B

# Glow effect colors
$glowColor = rgb({color6.strip})
$glowColorInactive = rgb({color8.strip})
